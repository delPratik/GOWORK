CREATE TABLE client (
    client_id INT AUTO_INCREMENT,
    name VARCHAR(255),
    age INT,
    address VARCHAR(255),
    phone_number VARCHAR(20),
    email VARCHAR(255),
    password VARCHAR(255),
    PRIMARY KEY (client_id)
);
