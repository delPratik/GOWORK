CREATE TABLE freelancer (
    freelancer_id INT AUTO_INCREMENT,
    name VARCHAR(255),
    age INT,
    address VARCHAR(255),
    phone_number VARCHAR(20),
    email VARCHAR(255),
    password VARCHAR(255),
    skills VARCHAR(255),
    PRIMARY KEY (freelancer_id)
);
